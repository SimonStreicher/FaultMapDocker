Using FaultMap docker image on Ubuntu inside VirtualBox

Getting the image

After setting up Ubuntu Guest Additions and proxy settings run the following command:

sudo docker pull simonstreicher/faultmap



